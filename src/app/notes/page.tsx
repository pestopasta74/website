export default async function NotesPage() {
    // console.log(notes);

    return (
        <div>
            <h1 className="text-xl font-bold text-red-500 w-full text-center bg-blue-200 py-2">Notes Page</h1>
            <p>This is the notes page content.</p>
        </div>
    );
}
